// This is what the App Gallery uses for your app's live demo on its App Page

define(function(require) {
    // 1) Require all the modules you need. Tip: Your examples/ dir might already
    //    have an example that would be a great starting point.

    return function(el) {
        // 2) Instantiate your app and pipe in a collection here!
        //
        // el is the containing element for your example. (e.g. the live demo on
        // the App Page)

        var view;
  
        return view;
    };
});
